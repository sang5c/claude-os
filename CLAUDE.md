# CLAUDE.md

이 프로젝트는 "클로드 OS"를 직접 만들어보는 실습 공간입니다.

## 작업 규칙

1. **클로드 OS 관련 모든 파일은 반드시 프로젝트 안에 만들 것**
   - 예: `.claude/` 하위의 `*.md` 파일 등 클로드 OS를 구성하는 모든 산출물은 이 프로젝트 디렉토리 내부에 생성합니다.
   - 사용자 홈(`~/.claude/`)이나 프로젝트 바깥 경로에는 생성하지 않습니다.

2. **친절하게 설명하며 협업할 것**
   - 이 프로젝트는 클로드 OS 만들기 실습이므로, 사용자가 대화 과정을 통해 AI와의 협업 방식을 배울 수 있도록 합니다.
   - 무엇을, 왜 그렇게 하는지 과정을 설명하면서 진행합니다.
   - 단순히 결과만 내놓기보다, 의사결정의 이유와 대안을 함께 공유합니다.

3. **이 저장소의 모든 git/GitHub 작업은 sang5c 계정으로만 할 것**
   - commit author: `sang5c` / `skah321@gmail.com` (저장소 로컬 설정).
   - 인증: 전역 `gh auth switch`로 활성 계정을 바꾸지 않는다(다른 저장소에 영향). 저장소 로컬 credential 헬퍼가 런타임에 `gh auth token --user sang5c`로 sang5c 토큰을 가져와 인증한다.
   - 다른 저장소에는 절대 영향을 주지 않도록, 모든 계정 관련 설정은 `--local`로만 적용한다.

4. **작게 시작해 점진적으로 발전시킬 것**
   - 처음부터 완성형을 만들지 않는다. 가장 작은 쓸모 있는 단위를 먼저 만들고 실제로 돌려본다.
   - 돌려보며 드러난 부족한 부분을 다음 단계에서 보강한다.
   - 큰 설계를 한 번에 박아넣기보다, 검증된 작은 조각을 쌓아 올린다.

5. **커밋 메시지는 `docs/commit-style.md`의 컨벤션을 따를 것**
   - 형식 `<티켓번호> <type>: <요약>` (티켓 없으면 생략). 자세한 규칙은 해당 파일 참조.
